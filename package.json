{
  "name": "lagencesauvage-site",
  "version": "1.0.0",
  "description": "Site web L'Agence Sauvage avec intégration Notion",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "curl -L https://github.com/gohugoio/hugo/releases/download/v0.139.0/hugo_extended_0.139.0_Linux-64bit.tar.gz | tar xz && ./hugo --minify && cp -r public/* . || true",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "nodemailer": "^6.9.16",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
